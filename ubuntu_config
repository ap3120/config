echo press impécr syst to boot from USB
gnome-extensions disable ubuntu-dock@ubuntu.com
sudo apt update
sudo apt upgrade

#packages
sudo apt install python3 vim vim-gtk libreoffice git npm curl

#install node version manager nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash
#update the .bashrc
source ~/.bashrc
nvm ls-remote # show all available versions
nvm install <version> # for example nvm install 16
